name: Apply Academy lesson insert RLS fix

on:
  workflow_dispatch:
  push:
    branches:
      - cursor/academy-lesson-save-options-51a2
    paths:
      - admin/supabase-migration-academy-lesson-insert-fix.sql
      - .github/workflows/apply-academy-lesson-insert-fix.yml

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply insert RLS fix
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: rivjkiksknnesahrvamf
        run: |
          chmod +x scripts/apply-academy-live-migration.sh
          scripts/apply-academy-live-migration.sh admin/supabase-migration-academy-lesson-insert-fix.sql
